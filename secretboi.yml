---
- hosts: secretboi
  roles:
    # - base
    # - iptables
    - deluged

  tasks:
    - file: path="{{ item }}" state="directory" owner="root" group="root" mode="755"
      become: true
      with_items:
        - /data
        - /data/downloads
